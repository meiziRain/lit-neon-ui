:host {
    display: block;
    position: fixed;
    left: 50%;
    top: 0;
    .neon-toast {
        transform: translateX(-50%);
        min-width: 380px;
        box-sizing: border-box;
        border-radius: 8px;
        border: var(--neon-toast-border-blue);
        color: var(--neon-text-color);
        background-color: var(--neon-background-color);
        overflow: hidden;
        padding: 15px 15px 15px 20px;
        display: flex;
        align-items: center;
    }
}

[neon-color-scheme="light"] :host{
    .neon-toast {
        box-shadow: 0 2px 12px 0 rgb(0 0 0 / 10%);
    }
}
