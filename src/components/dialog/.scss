:host {
    & ::selection {
        color: #d94f5c;
    }
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    width: 100vw;
    height: 100vh;
    opacity: 0;
    pointer-events: none;

    #dialog-container {
        position: relative;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        width: 100vw;
        height: 100vh;
    }
    #dialog-wrapper {
        position: relative;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        width: 50vw;
    }

    neon-card {
        height: 100%;
    }

    #close {
        position: absolute;
        bottom: 0;
        right: 0;
    }

    #header {
        height: 60px;
    }

    #closer {
        position: absolute;
        right: 20px;
        top: 20px;
        font-size: 1.4em;
        display: inline-block;
        text-align: right;
    }

    ::slotted(*) {
        padding-top: 20px;
        text-align: right;
    }

    #title {
        font-size: 1.5em;
        text-align: left;
    }

    [center] {
        & #title,
        & ::slotted(*) {
            text-align: center;
        }
    }
}

:host([open]) {
    pointer-events: auto;
    opacity: 1;
    transform: none;
}
