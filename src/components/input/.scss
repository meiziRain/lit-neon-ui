:host {
    width: 480px;
    display: inline-block;
    input {
        -webkit-appearance: none;
        background-color: var(--neon-background-color);
        background-image: none;
        // border: var(--neon-border-blue);
        border: 2px solid rgb(134, 134, 134);
        // box-shadow: 0 0 20px rgba(198, 97, 97, 0.5), inset 0 0 20px rgba(198, 97, 97, 0.5);
        box-sizing: border-box;
        color: #606266;
        font-size: inherit;
        height: 40px;
        line-height: 1;
        outline: none;
        padding: 0 15px;
        transition: border-color 0.2s cubic-bezier(0.645, 0.045, 0.355, 1);
        width: 100%;
    }

    .neon-input {
        position: relative;
        font-size: 14px;
        width: 100%;
    }

    .neon-input:hover #input-suffix{
        visibility: visible;
        transition: all .5s linear;
    }

    #input-suffix {
        position: absolute;
        right: 10px;
        top: 50%;
        font-size: 20px;
        transform: translateY(-50%);
        text-align: center;
        color: #c0c4cc;
        transition: all 0.3s;
        pointer-events: none;
        visibility: hidden;
    }
}
